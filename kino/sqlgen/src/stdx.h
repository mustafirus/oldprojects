#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <malloc.h>
#include <string.h>
#include <limits.h>
#include <iostream>
#include <map>
#include <list>
#include <string>
#include <vector>
#include <algorithm>
#include <ios>
#include <stdexcept>


#define ASSERT(X)
#define DbError2(X,Y,Z)
/*
typedef unsigned long DWORD;
typedef unsigned int UINT;
typedef unsigned int HWND;
struct tag_SZ {
	int cx;
	int cy;
};
typedef tag_SZ SIZE;
#define min(X, Y)  ((X) < (Y) ? (X) : (Y))
#define max(X, Y)  ((X) > (Y) ? (X) : (Y))
#define _MAX_PATH	1024
*/
